package tv.anion.tv.update

/**
 * Свой апдейтер поверх GitHub Releases (PLAN Э6): нет стора — нет автообновления,
 * а сломанный резолвер делает мёртвыми все установки разом (PLAN §6).
 *
 * TODO(Э6): checkLatest() -> Release?, download(progress), installViaPackageInstaller()
 */
