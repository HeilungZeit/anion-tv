package tv.anion.tv.ui

/**
 * Сколько данные каталога и карточки тайтла считаются свежими.
 *
 * ViewModel'и живут в Activity, а Activity на телевизоре не умирает от выхода
 * на главный экран приставки: без срока годности однажды загруженный список
 * серий висел до полного закрытия приложения, и вчерашняя серия не появлялась
 * вовсе. Пара минут закрывает главный сценарий — «зашёл, вышел, вернулся» — и
 * при этом не превращает каждое «назад» в новый запрос.
 */
internal const val CONTENT_TTL_MS = 5 * 60_000L
